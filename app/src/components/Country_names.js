import React from "react";

const Country_names = () =>{

    const style = {
        with: "100%",
        minHeight: "400px",
        background:"red"
      }

    return(
        <div>
            {/* <h3>Country: { country.name }</h3>
            <h3>Population: { country.pop }</h3> */}
        </div>
    )
}